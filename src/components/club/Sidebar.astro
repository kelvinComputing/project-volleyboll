<!-- Overlay para móviles -->
<div id="sidebar-overlay" class="hidden fixed inset-0 bg-black bg-opacity-50 z-40 lg:hidden" onclick="toggleSidebar()"></div>

<!-- Sidebar -->
<div id="sidebar" class="hidden sm:flex flex-col fixed lg:relative z-50 bg-white h-full sidebar-transition 
                        sm:w-16 md:w-16 lg:w-64 transition-all duration-300">

  <!-- Contenido del sidebar -->
  <div class="p-4 md:p-6 flex-1 overflow-y-auto">
    <!-- Logo -->
    <div class="flex items-center justify-center lg:justify-start space-x-0 lg:space-x-2 mb-8">
      <i data-lucide="trophy" class="h-8 w-8 text-blue-600"></i>
      <span class="hidden lg:inline text-xl font-bold text-gray-900">VolleyClub</span>
    </div>

    <!-- Navegación -->
    <nav class="space-y-2">
      <button onclick="showModule('dashboard')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg bg-blue-600 text-white hover:bg-blue-700 transition-colors">
        <i data-lucide="layout-dashboard" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Dashboard</span>
      </button>
      <button onclick="showModule('players')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
        <i data-lucide="users" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Jugadores</span>
      </button>
      <button onclick="showModule('reports')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
        <i data-lucide="file-text" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Reportes</span>
      </button>
      <button onclick="showModule('calendar')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
        <i data-lucide="calendar" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Calendario</span>
      </button>
      <button onclick="showModule('club')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
        <i data-lucide="building" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Club</span>
      </button>
      <button onclick="showModule('payments')" class="nav-item w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
        <i data-lucide="credit-card" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
        <span class="hidden lg:inline">Pagos</span>
      </button>
    </nav>
  </div>

  <!-- Configuración -->
  <div class="p-4 lg:p-6">
    <button class="w-full flex items-center px-4 py-2 text-left rounded-lg text-gray-700 hover:bg-gray-100 transition-colors">
      <i data-lucide="settings" class="h-5 w-5 mx-auto lg:mr-3 lg:ml-0"></i>
      <span class="hidden lg:inline">Configuración</span>
    </button>
  </div>
</div>
