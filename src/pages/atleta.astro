---
import Layout from '../layouts/Layout.astro';

const userSession = Astro.cookies.get('serial_club');

if (!userSession) return Astro.redirect('/');

if (userSession) {
   if (userSession.value !== 'sinSerial') {
		return Astro.redirect('/club');
	}
}
---

<Layout>
</Layout>
